# MCP Agent Chatbot

Streamlit chatbot powered by LangChain, OpenAI (gpt-5-nano), and MCP tools.

## Setup

- **OPENAI_API_KEY**: Required for the LLM (set in `.env`).
- **API_KEY**: Required for the MCP server at `devws.vigilnz.com` (set in `.env`).

## Run

```bash
streamlit run app.py
```
